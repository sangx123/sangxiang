<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="winway.mdr.chinaunicom.activity"
      android:versionCode="2"
      android:versionName="1.4">
    <uses-sdk android:minSdkVersion="4" />
   <!-- 应用程序活动的相关配置(Activity) -->
    <application android:icon="@drawable/icon" android:label="@string/app_name">
    <!-- 单元测试 -->
    <uses-library android:name="android.test.runner" />
                                 <!-- 启动界面 -->
        <activity android:name="FirstEntryActivity"
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:screenOrientation="portrait"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
       
             <activity android:name="FunctionIntroductionActivity"
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:screenOrientation="portrait"/>
        <!--  -->
          <activity android:name="MainActivity"
              android:windowSoftInputMode="stateHidden|adjustResize"
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:screenOrientation="portrait"/>
                       <!--主界面 -->
                   <activity android:name="WelcomeActivity"
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:screenOrientation="portrait"/>
                                 <!-- 非急勿扰界面 -->
        <activity android:name="FJWRActivity"
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:screenOrientation="portrait"/>
                                 <!-- 定时设置界面 -->
        <activity android:name="TimeSettingActivity"
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:screenOrientation="portrait"/>
                                <!-- 定时设置界面(详细) -->
        <activity android:name="TimeSettingDetailActivity"
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:screenOrientation="portrait"/>
                                <!-- 时间设置自定义对话框 -->
        <activity android:name="BeginEndSettingActivity"
                  android:theme="@android:style/Theme.Dialog"
                  android:screenOrientation="portrait"/>
                  
                                  <!-- 黑白名单列表界面 -->
        <activity android:name="BlackWhiteListActivity"
         android:theme="@android:style/Theme.NoTitleBar"
                  android:screenOrientation="portrait"/>
                                    <!-- 修改密码 -->
        <activity android:name="EditPwdActivity"
         android:theme="@android:style/Theme.NoTitleBar"
                  android:screenOrientation="portrait"/>
                                   <!-- 设置配置界面 -->
        <activity android:name="SettingActivity"
         android:theme="@android:style/Theme.NoTitleBar"
                  android:screenOrientation="portrait"/>
                                <!-- 登录自定义对话框 -->
        <activity android:name="LoginActivity"
                  android:theme="@android:style/Theme.Dialog"
                  android:screenOrientation="portrait"/>
                                <!-- 登录自定义对话框 -->
        <activity android:name="AddBlackWhiteActivity"
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:screenOrientation="portrait"/>
                  
                                <!-- 快捷设置 -->
        <activity android:name="QuickSetupActivity"
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:screenOrientation="portrait"/>
          <activity android:name="PhoneListActivity"
                  android:theme="@android:style/Theme.Dialog"
                  android:screenOrientation="portrait"/>
                  <!-- 设置的详细信息 -->
          <activity android:name="ShowDetailActivity"
                  android:theme="@android:style/Theme.Dialog"
                  android:screenOrientation="portrait"/>
                                                          <!-- 业务设置 -->
        <activity android:name="YeWuActivity"
                  android:theme="@android:style/Theme.NoTitleBar"
                  android:screenOrientation="portrait"/>
                             <!-- 显示当前的状态 -->
          <activity android:name="winway.mdr.chinaunicom.activity.ThisCurrentStatusActivity"
                  android:theme="@android:style/Theme.Dialog"
                  android:screenOrientation="portrait"/>
                  
    </application> 
                             <!-- 应用程序所需要的权限 -->
                                 <!-- 读取联系人的权限 -->
       <uses-permission android:name="android.permission.READ_CONTACTS" />
       <uses-permission android:name="android.permission.INTERNET" /><!-- 访问网络 -->
	   <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /><!-- 查看当前网络的状态 -->
	   <uses-permission android:name="android.permission.SEND_SMS"/><!-- 发短信的权限 -->
	      <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
	  <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
	   <!-- 单元测试模块 -->
	   <uses-permission android:name="android.permission.RUN_INSTRUMENTATION" />
<instrumentation android:name="android.test.InstrumentationTestRunner" android:targetPackage="winway.mdr.chinaunicom.activity"
android:label="Test for my app"/>
</manifest>